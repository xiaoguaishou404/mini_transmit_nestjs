# 当前test文件用于测试项目build后在本地的运行情况
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 只安装生产依赖
RUN npm ci --only=production

# 复制构建好的应用
COPY dist ./dist

# 暴露端口
EXPOSE 3001

# 启动生产服务器
CMD ["node", "dist/main"] 